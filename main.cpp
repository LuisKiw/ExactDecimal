//main.cpp
//not really important but necessary
#include<iostream>
#include "exactdecimal.h"

int main(){
    //creating the floats
    float a = 0.1;
    float b = 0.2;
    //useless bool, I might delete it
    bool ready = true;
    CalculateFloat(ready, a, b);
    return 0;
}
